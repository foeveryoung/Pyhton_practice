#random
import random
random.random() #用于生成一个0至1之间的随机浮点数
random.uniform(2, 3)#用于生成一个指定范围内的随机浮点数
random.randint(2,4)#用于生成一个指定范围内的整数
random.randrange(10,100,2)#从10,12,14,16，。。。。，96,98获取一份随机数
random.choice("Python大护法")#从序列随机获取一个元素    ‘法’

list1=[1,2,3,4,5,6]
random.shuffle(list1) #打乱顺序  【6,1,5,3,2,4】

list2=[i**2 for i in range(0,10)]  #0,1,4,9,16,,,,81
random.sample(list2,3)   #[16,1,36]
